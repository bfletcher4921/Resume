# TODO: insert all comment blocks (see homework_program.py)
# Author: Brandi Fletcher
# Section: CIS 225 01 
# Date: 11/14/2023
# File: movies.py

'''
Instructions: Create a program that will display all of the top movies
for the given year.

You must implement the API that calls for two functions as shown below.
'''
def read_file():
    f = open("movies.txt")
    
def movie_titles(movie_titles):

def movie_years(movie_years):


def main():

    # create empty lists
    movie_titles =
    movie_years = 
    # read file and populate lists
    read_file("movies.txt", movie_titles, movie_years)

    while True:
        # prompt: Enter the year to display or ['quit']:
        year = input("Enter the year to display or ['quit']")
        # if input is invalid (not a number), display "invalid input"
        if movie_year.isdigit:
            print("invalid input")        
        # display movies for the given year (all 3 arguments are inputs)
        # Movies realeased in YYYY:
        # movie title 1
        # movie title 2
        # etc.
        display_movies(year, movie_titles, movie_years)

